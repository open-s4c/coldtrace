diff --git a/.github/workflows/actions-benchmark.yaml b/.github/workflows/actions-benchmark.yaml
index f4a2103..435daa7 100644
--- a/.github/workflows/actions-benchmark.yaml
+++ b/.github/workflows/actions-benchmark.yaml
@@ -23,21 +23,18 @@ jobs:
           sudo apt-get update
           sudo apt-get install -y miller hyperfine cmake make python3
 
-      - name: Download previous benchmark results
+      - name: Download previous benchmark results from main
         env:
           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         run: |
-          # Extract the current branch name (works for pushes and PRs)
-          BRANCH_NAME="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
-          
-          # Find the last successful workflow run ID on this branch
-          LAST_RUN=$(gh run list --branch "$BRANCH_NAME" --workflow "Run Benchmarks" --status success --json databaseId --jq '.[0].databaseId')
+          # Find the last successful workflow run ID on the main branch
+          LAST_RUN=$(gh run list --branch main --workflow "Run Benchmarks" --status success --json databaseId --jq '.[0].databaseId')
           
           if [ -n "$LAST_RUN" ] && [ "$LAST_RUN" != "null" ]; then
-            echo "Found previous successful run: $LAST_RUN"
+            echo "Found previous successful run on main: $LAST_RUN"
             gh run download $LAST_RUN --name benchmark-results --dir previous_results || echo "No previous artifact found."
           else
-            echo "No previous successful run found on branch $BRANCH_NAME."
+            echo "No previous successful run found on the main branch."
           fi
 
       - name: Build project
@@ -64,14 +61,14 @@ jobs:
             echo "Comparing $PREV_SUMMARY against $CURR_SUMMARY"  
             python3 scripts/bench-comparison.py "$PREV_SUMMARY" "$CURR_SUMMARY" >> $GITHUB_STEP_SUMMARY
           else
-            echo "Skipping comparison: No previous results available."
+            echo "Skipping comparison: No previous baseline from main available."
           fi
 
-      - name: Save current results as Artifact for the next run
+      - name: Save current results as Artifact (Main branch only)
         uses: actions/upload-artifact@v4
-        if: always() 
+        # Save the baseline only if we are on main AND the workflow hasn't failed
+        if: github.ref == 'refs/heads/main' && success()
         with:
           name: benchmark-results
           path: results/
           overwrite: true
-          retention-days: 30
diff --git a/scripts/bench-comparison.py b/scripts/bench-comparison.py
index b0b9b0c..3461d44 100644
--- a/scripts/bench-comparison.py
+++ b/scripts/bench-comparison.py
@@ -1,7 +1,7 @@
 import sys
 
 CHECK_VARIANTS = (".coldtrace", ".nowrites")
-ERROR = 0.03  # threshold for regression
+ERROR = 0.05  # threshold for regression
 
 def parse_results(filepath):
     """
diff --git a/scripts/benchmark.sh b/scripts/benchmark.sh
index 06b2806..ccb8cd4 100755
--- a/scripts/benchmark.sh
+++ b/scripts/benchmark.sh
@@ -103,13 +103,8 @@ echo "--> Generating Environment Metadata"
 {
     echo "## Environment"
     echo
-    echo "- Host: $HOST"
-    echo "- Date: $DATE"
-    echo "- Tag:  $(git rev-parse --short HEAD 2>/dev/null || echo 'N/A') ($(git branch --show-current 2>/dev/null || echo 'detached'))"
-    echo
-    echo '```'
-    uname -a | fmt -w 70
-    echo '```'
+    echo "- OS:  $(uname -srm)"
+    echo "- Tag: $(git rev-parse --short HEAD 2>/dev/null || echo 'N/A') ($(git branch --show-current 2>/dev/null || echo 'detached'))"
 } >> "$SUMMARY"
 
 # --- PROCESS & APPEND BENCHMARK RESULTS ---
